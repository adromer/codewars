function solution(str){
  
 
    return str.split("").reverse().join("");
    
    //split, reverse and join. Split turns characters into array of separate characters, reverse rearranges and join makes them a string again. Boom.

  
}
